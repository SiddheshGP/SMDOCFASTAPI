{
  "consumer_id": "partnerA",
  "metadata": {
    "orderId": 12345,
    "priority": "high",
    "tags": ["vip", "express"],
    "customerInfo": {"name": "John", "region": "APAC"},
    "items": [{"sku": "ABC", "qty": 2}],
    "riskAddress": {
      "address1": "Some Address",
      "street": "Some Street",
      "lane": "Some Lane",
      "city": "Some City",
      "postcode": 123456
    }
  }
}

    @model_validator(mode="after")
    def ensure_required_fields(self):
        missing = []
        if not self.externalRefId:
            missing.append("externalRefId")
        if not self.fileSource:
            missing.append("fileSource")
        if missing:
            raise ValueError(f"Missing required fields: {', '.join(missing)}")
        return self